<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- White background -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/white" />
            <corners android:radius="10dp" /> <!-- Adjust radius to your preference for rounded corners -->
        </shape>
    </item>
    <!-- Gold ring -->
    <item>
        <shape android:shape="rectangle">
            <stroke
                android:width="2dp"
                android:color="@color/loading" /> <!-- Gold color -->
            <corners android:radius="10dp" /> <!-- Adjust radius to your preference for rounded corners -->
        </shape>
    </item>
</layer-list>
